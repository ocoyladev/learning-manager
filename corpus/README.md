# Frozen corpus

The repository contains auditable local fixtures captured on 2026-08-31 for deterministic
replay. Live web capture was unavailable in this environment, so these fixtures do not claim
to reproduce the linked websites. Run `python -m learning_manager.cli build-corpus --topics-file
eval/corpus_topics.yaml` when network access is available and review the capture before freezing.
